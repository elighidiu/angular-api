import { ReadMorePipePipe } from './read-more-pipe.pipe';

describe('ReadMorePipePipe', () => {
  it('create an instance', () => {
    const pipe = new ReadMorePipePipe();
    expect(pipe).toBeTruthy();
  });
});
